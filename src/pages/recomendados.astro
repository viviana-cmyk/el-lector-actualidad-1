---
import Layout from "../layouts/Layout.astro";
import NewsGrid from "../components/NewsGrid.astro";
import UpdatedNote from "../components/UpdatedNote.astro";
import recomendadosData from "../data/news-recomendados.json";
import curated from "../data/curated.json";
---

<Layout title="Recomendados" description="Investigaciones de InSight Crime, International Crisis Group y el World Economic Forum, más libros recomendados por El LECTOR.">
  <section class="section wrap">
    <h2 class="section__title" data-i18n="investigations">Investigaciones</h2>
    <p class="section__byline">El LECTOR</p>
    <NewsGrid outlets={recomendadosData.outlets} />
    <UpdatedNote generatedAt={recomendadosData.generatedAt} />
  </section>

  <section class="section wrap">
    <h2 class="section__title" data-i18n="books">Libros</h2>
    <p class="section__byline">El LECTOR</p>
    <div class="reco-grid">
      {
        curated.books.map((book: any) => (
          <div class={`reco-card outlet--${book.color}`}>
            {book.tag && <span class="reco-card__tag">{book.tag}</span>}
            <h3>{book.title}</h3>
            <p class="reco-card__author"><strong>{book.author}</strong></p>
            <p>{book.description}</p>
          </div>
        ))
      }
    </div>
  </section>
</Layout>
